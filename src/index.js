import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  // Removed React.StrictMode to prevent double mounting and WebSocket connection issues
  <App />
);
